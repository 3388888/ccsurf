{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default window permissions. The app reads .bsp files through Rust commands, so no filesystem plugin is granted to the webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
